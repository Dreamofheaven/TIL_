박스가 있고, 여기에 값을 할당을 하고 있다고 했음 
그래서 결국에는 우리가 변수 이름을 짓고 있는 것이다.
여기에 가장 중요한 것은 타입이었다. 
객체의 다양한 타입들이 있다고 생각해라.
우리가 어떠한 타입들을 알아봤냐면, 우선은 불리언있고
int, float, sequence(string, list, range), collection(dictionary)
타입별로 조작할 수 있는 서로다른 내용들이 있는데, 
연산자나 혹은 메서드가 있다. 

문자열의 특징은 반복 가능하다.
숫자들의 나열, 리스트는 변경가능한 값들의 시쿼스 
레인지는 숫자들의 나열, 스트링은 문자들의 나얄 
딕셔너리는 키랑 밸류 

어떤 상황에서 서로 다른 실행드릉ㄹ 하고 싶다. 
딕셔너리를 이해하려면 우리가 뭔가를 넣는다는 관점에서 보면
약통, 월~일까지 아점저로 나뉜 약통은 너무 편하다. 

<파일 입출력>
파이썬을 활용하여 파일을 읽고 쓸 수 있다.
json 형식의 파일을 읽고 활용할 수 있다.
파일 확장자가 무슨 확장자들이 있나요?
-> hwp, txt, csv, pptx, py, c 

<파일 입력>
open(file, mode='r', encoding=None)
file은 파일명
mode는 텍스트 모드 
encoding은 인코딩 방식(일반적으로 utf-8활용)

'r' 리딩 / 'w'읽기 

<파일 활용>
>>> f = open('workfile', 'w')

>>> readline()

json은 자바스크립트 객체 표기법으로 개발환경에서 많이 활용되는 데이터 양식으로
웹 어플리케이션 뭐시기




