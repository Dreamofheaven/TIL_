for문은 순회 가능한 데이터 타입, list, dict, set, str, range

while문은 반복 구간이 안정해진 범위를 다룰 때

3. 문자열
문자열은 immutable(변경이 불가능한) 자료형
변수에 값 자체를 넣는게 아니라 메모리 주소를 넣는 거임

인덱싱, 음수인덱싱
리버스나 렌함수보다 인덱스 슬라이싱이 더 빠르다

문자열.startwith('특정문자')
문자열.endwith('특정문자')

