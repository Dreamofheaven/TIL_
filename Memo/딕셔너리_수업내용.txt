
<딕셔너리>

키-값 쌍으로 이뤄진 모음 
{키 : 값}

딕셔너리를 생성하고 관리를 하는 것인데, 개념적으로 가져가면 된다.
키는 변하면 안되기 때문에 리스트를 쓸 수 없다. 

딕셔너리에 접근하면, 키값만 반환한다.
왜냐면, 키만 주면 값에 접근할 수 있기 때문이다.
키값을 받고, 값에 접근하려면 어떤 코드를 사용해야하냐?
phone_book이라는 딕셔너리가 있음

for name in phone_book:
	print(phone_book[name])

<모듈과 패키지?
- 모듈 : 특정 기능을 하는 코드를 파이썬 파일(.pv) 단위로 작성한 것
- 패키지 : 특적 기능과 ~~

<파이썬 표준 라이브러리>

import os

print(os.listdir())

파이썬은 파일명을 다 바꿔줄 수 있음 
컴퓨터를 조작할 수도 있다. 
