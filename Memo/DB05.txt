SQL 치트 시트 

07. SQL - Multi Table Queries

1. Introduction to join
관계형 데이터베이스에서 외래 키(FK) 필드의 역할을 설명할 수 있다.
3가지 JOIN키워드

Q.왜 테이블을 여러개로 나눠서 관리를 할까?
예를 들어, 커뮤니티 게시판을 만들어야하는 경우
커뮤니티 게시판에 들어가야하는 데이터 기능이 뭐가 있을까?
-> 자유게시판에 필요한 데이터 생각해보기, 제목 내용 작성자 정보

* 권미자가 작성한 모든 게시글을 조회하기
SELECT * FROM 테이블 WHERE writer = '권미자';
어떤 문제점이 있을까?
-> 동명이인, 혹은 특정 데이터가 수정된다면(ex,
student가 영어로 되어있는데 갑자기 학생으로 바꾸려고 하려면?
이것에 연관된 수많은 데이터들이 있을텐데 그러면 다 바꿔야함
데이터의 관리가 너무 어려워진다. 

* 테이블을 나눠보자.
테이블을 나누면서 독립적으로 각각 흩어졌는데, 이 글을 누가 작성했고 이유저는 권한이
어떤 사람인지 알고 싶은데 독립된 테이블에서 다시 논리적인 연결을 만들려면
외래키가 필요하다. 다른 테이블의 특정 레코드릉ㄹ 명확하게 할 수 있는 pk값
실제로 연결이 된 건 아니지만 논리적인 관점에서 보면 참조를 하고 있는 것

유저테이블만보고 권한이 학생인 애들만 가져오기

테이블을 분리하면 관리는 용이하지만 출력할때는 테이브릉ㄹ 한개만 출력할 수 밖에
없어서 다른 테이블과 연결지어 출력하는 것이 필요하다. 
-> 그런 기능이 바로 JOIN

JOIN clause
-> 둘 이상의 테이블에서 데이터를 검색하는 방법

시작하기 전 데이터 준비

<JOIN 종류>
- INNER JOIN



- LEFT JOIN

- RIGHT JOIN
